 {
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "todo_list",
  "entities": [
    "src/models/*.{ts,js}"
  ],
  "migrations": [
    "src/database/migrations/*.{ts,js}"
  ],
  "cli": {
    "migrationsDir": "src/database/migrations"
  }
}
